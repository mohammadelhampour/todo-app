import React from "react"

const AddTaskForm = () => {
  return (
    <React.Fragment>
      <input type="text" placeholder="what needs to be done?"/>
      <button>Add</button>
    </React.Fragment>
  )
}

export default AddTaskForm